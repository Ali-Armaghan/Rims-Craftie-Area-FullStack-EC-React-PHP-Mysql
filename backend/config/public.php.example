<?php
// Copy to public.php on production host (optional but recommended for Vercel + API subdomain)

return [
    // Base URL where /uploads/... files are served (no trailing slash)
    'upload_origin' => 'https://api.craftiearea.local',
];
